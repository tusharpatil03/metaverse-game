import { Request, Response, NextFunction } from "express";

export const addSpace = (req: Request, res: Response, next: NextFunction) => {};

export const getSpace = (req: Request, res: Response, next: NextFunction) => {};

export const getAllSpaces = (req: Request, res: Response, next: NextFunction) => {};

export const addElement = (req: Request, res: Response, next: NextFunction) => {};

export const deleteElement = (req: Request, res: Response, next: NextFunction) => {};