import { Request, Response, NextFunction } from "express";

export const createElement = (req:Request, res:Response, next:NextFunction)=> {
    
}

export const updateElement = (req:Request, res:Response, next:NextFunction)=> {
    

}


export const createAvatar = (req:Request, res:Response, next:NextFunction)=> {
    
}

export const createMap = (req:Request, res:Response, next:NextFunction)=> {
    
}